from gimpfu import *


def plugin_main(input_img, output_img, background, alpha, output_size, mode, workers, verbose, nth,
                shapes):
    print(input_img, output_img, background, alpha, output_size, mode, workers, verbose, nth,
          shapes)
